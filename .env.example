# DockerDonkey configuration
PORT=8080
HOST=0.0.0.0

# Path to Docker socket on Ubuntu/Linux
DOCKER_SOCK=/var/run/docker.sock

# Directory for uploaded files (relative to project root)
UPLOAD_DIR=uploads

# Comma-separated list of allowed CORS origins; leave empty to allow all (not recommended for public)
# CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
